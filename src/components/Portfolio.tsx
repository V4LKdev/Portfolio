// Portfolio.tsx
// Main portfolio component using modern responsive design principles
// Single component that adapts to all screen sizes using CSS breakpoints

import * as React from 'react';
import ResponsivePortfolio from './ResponsivePortfolio';

const Portfolio = () => {
  // Modern responsive design - single component handles all screen sizes
  return <ResponsivePortfolio />;
};

export default Portfolio;
